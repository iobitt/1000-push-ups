# frozen_string_literal: true

source 'https://rubygems.org'

gem 'rake', '~> 13.0'

# ORM
gem 'activerecord'

# HTTP client
gem 'rest-client'

# postgres db driver
gem 'pg'

# wrapper for Telegram's Bot API
gem 'telegram-bot-ruby'

gem 'i18n'

gem 'pry'

group :development, :test do
  gem 'rubocop', '~> 1.27', require: false
end

group :development do
  gem 'byebug'
end
